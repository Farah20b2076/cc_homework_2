import java.util.Scanner;

public class Hw2
{
	
	public static void main(String[] args){
		
		Scanner s = new Scanner(System.in);                         // Scanner with variable, s. Useful to read input from user.
		
		System.out.println("Enter number: ");    
		int num = s.nextInt();                                      // Input number would be assigned to num variable with type int.
		
		float remainder= num%2;                                     // The working to see if input is divisible by 2, result is assigned to 
		                                                            // remainder variable in float type. 
		if (remainder == 0) {                                       
			System.out.println("The number is divisible by 2");       // If the input number from user can be divided by 2, it will print this out.
		} else {                                                    
			System.out.println("The number is NOT divisible by 2");   // If the input number from user can not be divided by 2, it will print this out.
		}
		
	}
}

// Relevant comments for certain lines
// formatting clean, if else function has their own block away from irrelevant blocks. 
// Other blocks/lines are only close to those that are closely relevant to them
